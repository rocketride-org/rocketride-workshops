{
  "name": "@workshops/coding-agent-solution",
  "version": "0.0.0",
  "private": true,
  "description": "Reference implementation for the coding-agent workshop.",
  "rocketride": {
    "runtime": "latest"
  },
  "scripts": {
    "postinstall": "launchpad install",
    "dev": "pnpm --filter \"./*\" --parallel run dev"
  },
  "devDependencies": {
    "@rocketride/runtime": "workspace:*"
  }
}
