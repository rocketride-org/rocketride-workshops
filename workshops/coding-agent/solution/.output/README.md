# Hello World

A minimal Node.js application that prints "Hello, World!" to the console.

## Prerequisites

- [Node.js](https://nodejs.org/) (v14 or later)

## Usage

Run the app with npm:

```bash
npm start
```

Or run it directly with Node:

```bash
node index.js
```
