{
  "name": "@workshops/coding-agent-exercise",
  "version": "0.0.0",
  "private": true,
  "description": "Scaffold for the coding-agent workshop. Fill out the TODOs in api/app/pipelines/.",
  "rocketride": {
    "runtime": "latest"
  },
  "scripts": {
    "postinstall": "launchpad install && uv sync --directory api --all-groups",
    "dev": "pnpm --filter \"./*\" --parallel run dev"
  },
  "devDependencies": {
    "@rocketride/runtime": "workspace:*"
  }
}
