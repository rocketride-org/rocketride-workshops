# Pipeline definitions

Each `*.py` file in this folder defines a Rocketride pipeline that the runtime
loads and executes. For the workshop hello-world exercise, create a `hello.py`
that defines a trivial pipeline returning a string. Later steps will replace
that with a real coding-agent pipeline (LLM call, tool use, etc.).

The `solution/` directory has a complete example.
